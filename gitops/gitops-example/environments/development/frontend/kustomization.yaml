apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/frontend
  - config.yaml

patches:
  - target:
      kind: Deployment
      name: frontend
    path: patches.yaml

images:
  - name: cdtroman/gitops-example-frontend
    newTag: v0.1.0
